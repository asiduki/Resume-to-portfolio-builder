# 🚀 Future Additions

Features that are planned but not yet in work. When one of these is picked up, move it into the app and remove it from this list.

## 📊 Portfolio Analytics (removed from app — was a placeholder)

Previously lived at `/dashboard/analytics` as a "coming soon" page, linked from the sidebar, the dashboard quick actions, and the owner toolbar on the public portfolio page.

Planned scope:

- Portfolio page views (total + over time)
- Unique visitors
- Traffic sources / referrers
- Per-section engagement insights

To re-add: restore `app/dashboard/analytics/page.tsx`, `components/portfolio/AnalyticsButton.tsx`, and the nav entries in `components/dashboard/DashboardShell.tsx`, `app/dashboard/page.tsx` (quick actions), and `components/portfolio/OwnerToolbar.tsx` (Manage group).

## 📌 Other Ideas (from README roadmap)

- Resume PDF Generator
- Theme Customization
- Custom Domains
- Portfolio SEO Enhancements
- AI Content Improvement
- Drag & Drop Portfolio Editor
- Portfolio Version History
- More settings on the Settings page (currently only links to Profile)
